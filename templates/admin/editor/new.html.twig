{% extends 'base.html.twig' %}

{% block title %}Ajouter un éditeur{% endblock %}

{% block body %}

<div class="container">
    <h1 class="text-center my-5">Ajouter un éditeur</h1>
    <div class="w-50 m-auto">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Ajouter</button>
        {{ form_end(form) }}
    </div>
</div>

{% endblock %}
